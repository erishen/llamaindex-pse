.env
__pycache__
*.egg-info
outputs/
.venv

# 生成的简历（含个人信息，禁止入库；按模型 provider 区分文件名）
tailored_resume_*.md
recommended_resume_*.md

# 含个人隐私的 prompt 文件（公司名/项目时间/GitHub/指标等）
tasks/*/prompts/recommend_specialist.md

# Embedding 索引缓存（可本地重建，不入库）
.index_cache/

# 脱敏规则（含真实标识符，本地生效，禁止入库）
tasks/*/privacy_patterns.json
.serena

# ── hot-news 任务：本地生成/敏感的产物不入库 ──
# 抓取到的第三方新闻原文快照（受版权保护、可再抓取）：不入库
tasks/hot-news/news/
# 已生成的文章成稿（带时间戳、可再生成）：不入库
tasks/hot-news/articles/
# 发布器（Playwright 平台自动化/风控绕过脚本）+ 登录态 + 品牌图：整体不入库
tasks/hot-news/publish/
